# Runtime pip dependencies for the evaluation package and the Docker images
# (docker/Dockerfile.api, docker/Dockerfile.worker).
# Kept in sync with the pip section of environment.yml. GPU-only packages
# (torch, transformers, accelerate) are installed separately in Dockerfile.worker.

# Scientific / statistical core
numpy>=1.24
scipy>=1.10
pandas>=2.0
statsmodels>=0.14
scikit-learn>=1.3
lifelines>=0.27

# Plotting
matplotlib>=3.7
seaborn>=0.12
plotly>=5.0

# Web API
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0
flask>=2.3
flask-cors>=4.0
aiofiles>=23.0

# Templating / parsing / NLP helpers
jinja2>=3.1
pyyaml>=6.0
nltk>=3.8
numerizer>=0.2
tiktoken>=0.5
beautifulsoup4>=4.12
lxml>=4.9

# LLM provider SDKs
openai>=1.0
anthropic>=0.25
google-generativeai>=0.5

# Utilities
python-dotenv>=1.0
tqdm>=4.65

# Testing
pytest>=7.4
pytest-asyncio>=0.21
