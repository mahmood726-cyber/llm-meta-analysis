# llm-meta-analysis — environment template
#
# Copy this file to `.env` and fill in real values before running.
# Never commit your .env (it is gitignored).

# Google API key (e.g. for Gemini / Generative Language API)
GOOGLE_API_KEY=
